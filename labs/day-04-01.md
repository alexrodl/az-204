# 🧪 Día 04 - Azure SQL Database & Azure Cosmos DB

## 🎯 Objetivos

- Entender el modelo de datos relacional (SQL)
- Crear una base de datos en Azure SQL
- Conectarse desde una aplicación .NET
- Comprender el modelo NoSQL con Cosmos DB
- Identificar cuándo usar SQL vs NoSQL

---

# 🕐 Parte 1 - Azure SQL Database (Relacional)

## 🧠 Conceptos clave

- Base de datos relacional
- Tablas, filas, columnas
- SQL queries
- Connection string

---

## 🧩 Introducción

Azure SQL Database es un servicio PaaS basado en SQL Server.

👉 Se utiliza para datos estructurados con relaciones.

---

## 🖥️ Demo (Portal)

Vamos a crear:

- SQL Server
- Azure SQL Database

👉 Conceptos importantes:

- Server → contenedor lógico
- Database → base de datos
- Autenticación (usuario/password)

---

## 🔐 Configuración importante

- Permitir acceso desde tu IP (firewall)
- Guardar connection string

---

## 💻 Demo (.NET)

Crear una aplicación de consola en .NET y conectarse a la base de datos.

```powershell
dotnet new console
```

```powershell
dotnet add package Microsoft.Data.SqlClient
dotnet add package Microsoft.Data.SqlClient.Extensions.Azure
dotnet add package Azure.Identity
```

```powershell
az login
```


Ejemplo:

```csharp
using Microsoft.Data.SqlClient;

string serverName = "az-204-sqlserver.database.windows.net";
string connectionString = $"Server=tcp:{serverName},1433;Initial Catalog=az-204-database;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;Authentication=Active Directory Default;";

using (SqlConnection connection = new SqlConnection(connectionString))
{
    try
    {
        connection.Open();
        Console.WriteLine("Connection successful!");

        SqlCommand qry = new SqlCommand("SELECT * FROM SalesLT.Product", connection);
        SqlDataReader reader = qry.ExecuteReader();
        while (reader.Read())
        {
            Console.WriteLine($"{reader["ProductID"]}, {reader["Name"]}, {reader["ProductNumber"]}, {reader["Color"]}, {reader["StandardCost"]}, {reader["ListPrice"]}");
        }

    }
    catch (SqlException ex)
    {
        Console.WriteLine($"Connection failed: {ex.Message}");
    }
}

```